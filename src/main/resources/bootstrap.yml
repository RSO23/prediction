spring:
  application:
    name: prediction
  cloud:
    consul:
      host: localhost
      config:
        fail-fast: false
        format: yaml
      discovery:
        fail-fast: false
        prefer-ip-address: true

---
spring:
  profiles: prod
  cloud:
    consul:
      host: consul-consul-server
      config:
        fail-fast: true
      discovery:
        fail-fast: true